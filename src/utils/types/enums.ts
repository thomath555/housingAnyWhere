export enum RequestMethodType {
  POST = "POST",
  GET = "GET",
}

export enum FontSizes {
  default = 1,
  largeText = 4,
  medText = 1.5,
  subText = 0.8,
}

export enum FontWeights {
  default = 300,
  regular = 400,
  medium = 500,
  semiBold = 600,
  bold = 700,
}